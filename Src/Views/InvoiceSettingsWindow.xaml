<Window x:Class="AIT_ExcelAddIn_E_conomic.Views.InvoiceSettingsWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:local="clr-namespace:AIT_ExcelAddIn_E_conomic.Views"
             Title="Invoice Settings" Height="600" Width="600" MinWidth="600" SizeToContent="Height">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4" MinWidth="2"/>
            <ColumnDefinition Width="*" MinWidth="500"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Column Mapping" Grid.Row="0" Grid.Column="1" Margin="1">
            <StackPanel>
                <DataGrid x:Name="DataGridInvoiceColDefinition"
						  AutoGenerateColumns="False" Margin="1"
						  CanUserSortColumns="False" CanUserAddRows="False" CanUserReorderColumns="False"
						  ClipboardCopyMode="IncludeHeader" SelectionMode="Single" SelectionUnit="Cell"
						  GridLinesVisibility="None" CanUserResizeColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn x:Name="ColCustomerNumber" Binding="{Binding CustomerNumber}" Header="Customer Number" Width="Auto"></DataGridTextColumn>
                        <DataGridTextColumn x:Name="ColCustomerName" Binding="{Binding CustomerName}" Header="Customer Name" Width="Auto" ></DataGridTextColumn>
                        <DataGridTextColumn x:Name="ColLineItemPrice" Binding="{Binding LineItemPrice}" Header="Line Item Price" Width="Auto"></DataGridTextColumn>
                        <DataGridTextColumn x:Name="CultureDecimalDelimiter" Binding="{Binding CultureDecimalDelimiter}" Header="Decimal Symbol" Width="Auto"></DataGridTextColumn>
                    </DataGrid.Columns>
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="BorderBrush" Value="Gray"/>
                            <Setter Property="BorderThickness" Value="0.4"/>
                            <Setter Property="Background" Value="WhiteSmoke"></Setter>
                            <Setter Property="Height" Value="22"></Setter>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                            <Setter Property="Padding" Value="5,0,5,0"/>
                        </Style>
                        <Style TargetType="DataGrid">
                            <Setter Property="BorderBrush" Value="Gray"/>
                            <Setter Property="BorderThickness" Value="0.4"/>
                            <Setter Property="RowHeaderWidth" Value="0"></Setter>
                            <Setter Property="RowHeight" Value="30"></Setter>
                            <Setter Property="ColumnWidth" Value="*"></Setter>
                            <Setter Property="Background" Value="White"></Setter>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderBrush" Value="Gray"/>
                            <Setter Property="BorderThickness" Value="0.2"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Setter Property="TextBlock.TextAlignment" Value="Center" />
                        </Style>
                    </DataGrid.Resources>
                </DataGrid>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Column="1" Grid.Row="2" Margin="1">
            <GroupBox.Header>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Label Content="Description Column Mapping and Format"></Label>
                    <Button x:Name="BtnShowHelp" VerticalAlignment="Center" Width="20" Height="20" Click="BtnShowHelp_Click">?</Button>
                </StackPanel>
            </GroupBox.Header>
            <StackPanel Orientation="Vertical">
                <TextBox x:Name="TextBoxDescriptionDefinition" Text="{Binding Description}" HorizontalAlignment="Stretch" TextWrapping="Wrap"  Width="Auto" Margin="4" Height="52" AcceptsReturn="True"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Default Values" Grid.Column="1" Grid.Row="4" Margin="1">
            <StackPanel>
                <DockPanel Margin="4">
                    <Label Content="Layout" ToolTip="Invoice layout"/>
                    <ComboBox x:Name="ComboBoxLayouts" ItemsSource="{Binding Collection}" SelectedValuePath="LayoutNumber" DisplayMemberPath="Name" HorizontalAlignment="Right" Width="460" MinWidth="400" ToolTip="Default Layout if not defined on Customer"/>
                </DockPanel>
                <DockPanel Margin="4">
                    <Label Content="VAT Zone"/>
                    <ComboBox x:Name="ComboBoxVatZones" ItemsSource="{Binding Collection}" SelectedValuePath="VatZoneNumber" DisplayMemberPath="Name" HorizontalAlignment="Right" Width="460" MinWidth="400" ToolTip="Default VAT Zone if not defined on Customer"/>
                </DockPanel>
                <DockPanel Margin="4">
                    <Label Content="Payment Terms"/>
                    <ComboBox x:Name="ComboBoxPaymentTerms" ItemsSource="{Binding Collection}" SelectedValuePath="PaymentTermsNumber" DisplayMemberPath="Name" HorizontalAlignment="Right" Width="460" MinWidth="400" ToolTip="Default Payment Terms if not defined on Customer"/>
                </DockPanel>
                <DockPanel Margin="4">
                    <Label Content="Product"/>
                    <ComboBox x:Name="ComboBoxProduct" ItemsSource="{Binding Collection}" SelectedValuePath="ProductNumber" DisplayMemberPath="Name" HorizontalAlignment="Right" Width="460" MinWidth="400" ToolTip="Default Product"/>
                </DockPanel>
                <DockPanel Margin="4">
                    <Label Content="Unit"/>
                    <ComboBox x:Name="ComboBoxUnit" ItemsSource="{Binding Collection}" SelectedValuePath="UnitNumber" DisplayMemberPath="Name" HorizontalAlignment="Right" Width="460" MinWidth="400" ToolTip="Default Unit"/>
                </DockPanel>
                <DockPanel Margin="4">
                    <Label Content="Issue date"/>
                    <DatePicker x:Name="DatePickerInvoiceCreatedDate" HorizontalAlignment="Right" Width="460" MinWidth="400"/>
                </DockPanel>
            </StackPanel>
        </GroupBox>


        <!-- Save & Cancel Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="5" HorizontalAlignment="Right" Margin="4">
            <Button x:Name="BtnSave" Content="Save" Width="80" Margin="1" IsDefault="True" Click="BtnSave_Click"></Button>
            <Button x:Name="BtnCancel" Content="Cancel" Width="80" Margin="1" IsCancel="True" Click="BtnCancel_Click"></Button>
        </StackPanel>

        <StatusBar Grid.ColumnSpan="2" Grid.Row="6" Height="20" VerticalAlignment="Bottom">
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="TextBlockStatusBar" Margin="1 0 12 1"></TextBlock>
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>